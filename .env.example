# OpenChance Environment Variables

# Application
NODE_ENV=development
PORT=5173

# Database (when backend is implemented)
# DATABASE_URL=postgresql://user:password@localhost:5432/openchance

# Redis (for caching and sessions)
# REDIS_URL=redis://localhost:6379

# Email Service (for waitlist and contact forms)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password

# API Keys (when integrations are added)
# OPENAI_API_KEY=your-openai-key
# STRIPE_SECRET_KEY=your-stripe-key
# STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key

# Security
# JWT_SECRET=your-jwt-secret
# SESSION_SECRET=your-session-secret

# Analytics (optional)
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# MIXPANEL_TOKEN=your-mixpanel-token

